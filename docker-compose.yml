services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: fedstock-backend:local
    container_name: fedstock-backend
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      TZ: Asia/Seoul
    restart: unless-stopped
